# monitor model in production


def launch_monitoring(EXPERIMENT_NAME, is_prod):

    # check if the model monitoring is live
    # launch monitoring if not
    # monitoring should test if the model is still good with new data,
    # particularly on a set that didn't have good accuracy

    return True
